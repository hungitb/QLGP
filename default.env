NODE_ENV=production

# Overral
QLGP_REQUIRE_LOGIN=true
QLGP_USE_BACKEND=true

# Frontend (without backend if used) config

# Backend config (if used)
QLGP_BACKEND_PORT=4800
QLGP_SESSION_DURATION=30 # minutes
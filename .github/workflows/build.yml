name: CICD
on:
  push:
    branches:
      - main
jobs:
  build_and_push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push BE & FE docker image
        run: |
          docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/qlgp-be:latest .
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/qlgp-be:latest

      - name: Build and push fuseki docker image
        run: |
          docker build -t ${{ secrets.DOCKERHUB_USERNAME }}/qlgp-fuseki:latest fuseki
          docker push ${{ secrets.DOCKERHUB_USERNAME }}/qlgp-fuseki:latest
